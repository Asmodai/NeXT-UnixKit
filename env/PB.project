FILESTABLE = {
    OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.postamble);
    OTHER_LINKED = ();
    C_FILES = (env.c);
};
LOCALIZABLE_FILES = {
};

PROJECTVERSION = 1.1;
INSTALLDIR = "$(HOME)/bin";
PROJECTTYPE = Tool;
PROJECTNAME = env;
GENERATEMAIN = NO;
DOCICONFILES = ();
DOCEXTENSIONS = ();
