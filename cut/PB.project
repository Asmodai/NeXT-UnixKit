DOCEXTENSIONS = ();
LANGUAGE = English;
LOCALIZABLE_FILES = {
};
PROJECTVERSION = 1.1;
INSTALLDIR = "$(HOME)/bin";
PROJECTTYPE = Tool;
PROJECTNAME = cut;
FILESTABLE = {
    OTHER_SOURCES = (Makefile.preamble, Makefile, Makefile.postamble);
    OTHER_LINKED = ();
    C_FILES = (cut.c);
};
GENERATEMAIN = YES;
DOCICONFILES = ();
